# Analysis layer dependencies
#   python -m pip install -r requirements.txt
#
# No Firebase SDK is needed: the study runs without authentication and the
# Firestore rules allow anonymous reads, so records are fetched over the plain
# REST API with `requests`.

pandas>=2.0
numpy>=1.24
scipy>=1.11
matplotlib>=3.7
requests>=2.28

# Optional — only if you later restrict reads and need a service account:
# google-auth>=2.0
